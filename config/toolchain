#
#   Athix Kernel's Toolchain Configuration
#

# Toolchain prefix for target architecture
# This is used to specify the toolchain executables.
# Change this value based on the target architecture or cross-compiler prefix.
TOOLCHAIN_PREFIX := x86_64-elf

# Compiler and Linker Settings
# The following variables define the compiler, linker, and other tools to use for building the kernel.
CC 		:= $(TOOLCHAIN_PREFIX)-gcc          # C Compiler
CXX		:= $(TOOLCHAIN_PREFIX)-g++         	# C++ Compiler
LD 		:= $(TOOLCHAIN_PREFIX)-ld           # Linker
AS 		:= $(TOOLCHAIN_PREFIX)-as           # Assembler
AR 		:= $(TOOLCHAIN_PREFIX)-ar           # Archiver
NM 		:= $(TOOLCHAIN_PREFIX)-nm           # Symbol Table Manipulator
OBJCOPY := $(TOOLCHAIN_PREFIX)-objcopy  	# Object File Copy
OBJDUMP := $(TOOLCHAIN_PREFIX)-objdump  	# Object File Dump
SIZE 	:= $(TOOLCHAIN_PREFIX)-size        	# Size of binary
STRIP 	:= $(TOOLCHAIN_PREFIX)-strip      	# Symbol Stripping